# 💡 Pure CSS Multi-Lamp Brightness Controller

A **pure HTML & CSS project** that simulates **three hanging lamps inside a house**, each with **independent brightness control** using radio buttons.  
No JavaScript is used — all interactivity is handled through **CSS selectors and transitions**.

---

## 📌 Features

- ✅ **Three independent lamps**
- ✅ **6 brightness levels (0–5) per lamp**
- ✅ **Pure CSS interaction (no JavaScript)**
- ✅ **Smooth glow transitions**
- ✅ **Custom lamp colors**
- ✅ **House layout with hanging handles**

---

## 🧠 How It Works

- Each lamp uses a **group of hidden radio buttons**
- Clicking a label changes the selected radio button
- CSS `:checked` selectors update the lamp’s glow intensity
- Each lamp has its **own radio group**, so brightness controls don’t interfere




